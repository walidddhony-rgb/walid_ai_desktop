# Core runtime
PyQt6>=6.6
requests>=2.31
# NOTE: duckduckgo-search is the legacy name; the maintained successor is "ddgs".
# Keep this until you migrate the imports in search/, then switch to: ddgs>=9.0
duckduckgo-search>=6.1
PyMuPDF>=1.24
python-docx>=1.1

# Voice stack (heavy - Whisper models, TTS engines, audio I/O)
pyaudio>=0.2.13
openai-whisper>=20231117
faster-whisper>=1.0
pyttsx3>=2.90
edge-tts>=6.1
